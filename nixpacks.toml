[phases.setup]
nixPkgs = ["nodejs", "bun"]

[phases.install]
cmds = ["bun install"]

[phases.build]
cmds = ["cd packages/db && bun run prisma generate"]

[start]
cmd = "cd apps/server && bun run --hot src/index.ts"
