{
  "steps": {
    "install": {
      "commands": ["bun install"]
    },
    "build": {
      "commands": [
        "cd packages/db && bun run prisma generate"
      ]
    }
  },
  "deploy": {
    "startCommand": "cd apps/server && bun run --hot src/index.ts"
  }
}
