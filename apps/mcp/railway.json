{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "bun install && cd apps/mcp && bun run build",
    "watchPatterns": [
      "apps/mcp/**",
      "packages/**"
    ]
  },
  "deploy": {
    "startCommand": "cd apps/mcp && bun run start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
