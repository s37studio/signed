{
  "name": "@my-better-t-app/mcp",
  "type": "module",
  "version": "0.1.0",
  "main": "src/index.ts",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "start": "bun run src/index.ts",
    "build": "bun build --compile --minify src/index.ts --outfile mcp-server",
    "check-types": "tsc -b"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.27.1",
    "hono": "^4.8.2",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@my-better-t-app/config": "workspace:*",
    "@types/bun": "catalog:",
    "typescript": "catalog:"
  }
}
